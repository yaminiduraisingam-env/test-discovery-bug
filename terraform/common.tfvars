# common.tfvars
# Shared variable values referenced by all environments in env0-discovery.yaml.
# These values are intentionally minimal — this is a no-op test repo.

environment_name = "noop-discovery-test"
timestamp        = "2026-05-06"
